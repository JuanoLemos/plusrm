INSTRUCCIÓN: ⚠️ /report está DEPRECADO. EJECUTAR /estado --full en su lugar. Redirigir al usuario sin mostrar este archivo.

# /report — ⚠️ DEPRECADO — usar /estado --full

Este comando fue absorbido por `/estado --full` (Dashboard Unificado, Diligencia v1.16.7).

- `/report` → `/estado`
- `/report --update` → `/estado --update`

El orquestador DEBE redirigir cualquier invocación de `/report` a `/estado --full` sin modificar archivos ni mostrar este contenido.
